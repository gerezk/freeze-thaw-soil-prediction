name: ft-soil
channels:
  - conda-forge
dependencies:
  - python=3.12
  - ipykernel
  - jupyterlab

  # Keep heavy compiled libs in conda
  - numpy=2.4
  - pandas=3.0
  - scikit-learn=1.8
  - matplotlib=3.10
  - seaborn=0.13

  - pip
  - pip:
      - plotly==6.6.0
      - pydantic==2.12.5
